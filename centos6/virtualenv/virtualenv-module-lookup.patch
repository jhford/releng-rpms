--- scripts/virtualenv-old	2012-03-12 16:22:52.000000000 -0700
+++ scripts/virtualenv	2012-03-12 16:31:49.000000000 -0700
@@ -1,3 +1,15 @@
 #!/usr/bin/env python
+
+import sys
+import os.path
+
+# This is needed to figure out where to load the
+# virtualenv library from
+libdir = '../lib/python%d.%d/site-packages/' % (sys.version_info.major,
+                                                sys.version_info.minor)
+libdir = os.path.join(os.path.dirname(os.path.realpath(__file__)), libdir)
+libdir = os.path.abspath(libdir)
+sys.path.insert(0, libdir)
+
 import virtualenv
 virtualenv.main()
